<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Direcciones</title>
</head>
<body>
    <h1>LISTADO DE DIRECCIONES</h1>
    <ul>
        <% for( let i = 0; i < addresses.length; i++ ) { %>
        <li>
            <a href="address/detail/<%= addresses[i].id %>"><%= addresses[i].street %> <%= addresses[i].number %>, <%= addresses[i].city %>, <%= addresses[i].state %> - Usuario: </a>
        </li>
        <% } %>
    </ul>
    <div>
        <a href="/address/add">
            Crear direccion
        </a>
    </div>
</body>
</html>